import React from "react";
import { SignOutButton } from "@/app/auth/SignOutButton";

const ProfilePage = () => {
    return (
        <div>
            <h1>Profile</h1>
            <SignOutButton />
        </div>
    );
};

export default ProfilePage;